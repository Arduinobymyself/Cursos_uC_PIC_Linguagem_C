#include <main.h>

void main()
{
   port_b_pullups(true);
   while(TRUE)
   {
      if(input(pin_a0) == 0){
         output_high(pin_b0);
      }
      else
      {
         output_low(pin_b0);
      }
   }

}
