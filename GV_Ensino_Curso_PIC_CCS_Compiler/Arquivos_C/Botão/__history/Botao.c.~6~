#include <Botao.h>

void main()
{

   while(TRUE)
   {
      //TODO: User Code
      if(input(pin_a0 == 0)){
         delay_ms(300);
         output_high(pin_b0);
      } else {
         output_low(pin_b0);
      }
   }

}
